// Firebase authentication service stub
export const loginAdmin = async (email, password) => {
  throw new Error("Authentication service is currently mock and disabled.");
};

export const logoutAdmin = async () => {
  throw new Error("Authentication service is currently mock and disabled.");
};

export const getCurrentUser = () => {
  return null;
};
